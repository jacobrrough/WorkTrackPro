# Production build: set when running "npm run build" or in CI.
# For local production build testing, copy to .env.production with real values.
# For Netlify, set these in Site settings -> Environment variables.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
