# WorkTrack Pro - Railway
# Build: install deps + Vite build. Start: serve dist on PORT.

[build]
builder = "nixpacks"
buildCommand = "npm ci && npm run build"

[deploy]
startCommand = "npm start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
