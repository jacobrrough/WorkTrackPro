[build]
builder = "nixpacks"
# Use npm ci (no --include=dev) for npm 6/7+ compatibility; devDependencies are needed for Vite build
buildCommand = "npm ci && npm run build"

[deploy]
startCommand = "npm start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
