# WorkTrack Pro - Netlify
# Build and deploy from GitHub

[build]
  command = "npm run build"
  publish = "dist"

# SPA: all routes -> index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
