# production-safety

→ Check allocation guard (job_inventory trigger) and backup strategy. Ensure supabase/migrations/20260313000001_job_inventory_allocate_guard.sql is applied in Supabase; point to docs/BACKUP.md for PITR and manual backups.

This command will be available in chat with /production-safety
