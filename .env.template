# WorkTrack Pro Environment Configuration

# PocketBase Backend URL
# IMPORTANT: Replace 192.168.1.100 with YOUR computer's actual IP address!
# Find your IP:
#   Windows: ipconfig (look for IPv4 Address)
#   Mac: ifconfig | grep "inet " | grep -v 127.0.0.1
#   Linux: hostname -I

# Both services run on HTTPS for production-ready security
VITE_POCKETBASE_URL=https://192.168.1.100:8090

# Notes:
# - MUST use https:// for both services
# - MUST use your computer's IP address (not localhost) for mobile access
# - Camera will work on iOS with HTTPS
# - After changing this file, restart: npm run dev
