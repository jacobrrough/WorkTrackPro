# WorkTrack Pro Environment Configuration (Supabase)

# Supabase project URL and anon key (from Supabase Dashboard → Settings → API)
# Local dev: copy to .env.local and fill in. Netlify: set in Site → Environment variables.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Notes:
# - After changing env vars locally, restart: npm run dev
# - On Netlify, use "Clear cache and deploy" after changing variables
