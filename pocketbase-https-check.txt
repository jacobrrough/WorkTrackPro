import PocketBase from 'pocketbase';
import { User, Job, Shift, InventoryItem, Comment } from './types';

// Initialize PocketBase
// IMPORTANT: Uses HTTPS by default - make sure .env has https:// URL
const POCKETBASE_URL = import.meta.env.VITE_POCKETBASE_URL || 'https://localhost:8090';
const pb = new PocketBase(POCKETBASE_URL);

// Log connection info
console.log('PocketBase URL:', POCKETBASE_URL);
console.log('Protocol:', POCKETBASE_URL.startsWith('https') ? 'HTTPS ✓' : 'HTTP ⚠️');

// Enable auto-cancellation for duplicate requests
pb.autoCancellation(false);

// Rest of the file stays the same...
// (This is just the beginning - the actual file has all the service methods)
