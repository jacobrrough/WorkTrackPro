# Supabase (required for app)
# Get these from: Supabase Dashboard -> Project Settings -> API
# Copy this file to .env.local and replace with your real values. For Netlify, set these in Site settings -> Environment variables.
VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_key_from_supabase_dashboard

# Optional: Trello API credentials used only by the Trello importer
# These allow fallback attachment fetches when the exported JSON omits attachments.
VITE_TRELLO_API_KEY=your_trello_api_key
VITE_TRELLO_API_TOKEN=your_trello_api_token
